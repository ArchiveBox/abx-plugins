# Copy to .env before starting. Relative paths are resolved from this directory.
TLSNOTARY_STATE_DIR=./state
# Listen only on loopback; put an HTTPS reverse proxy or tunnel in front.
TLSNOTARY_PORT=7047
