{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Console Log",
  "description": "Capture browser console messages emitted while the page loads.",
  "type": "object",
  "additionalProperties": false,
  "required_plugins": [
    "chrome"
  ],
  "required_binaries": [
    {
      "name": "{CHROME_BINARY}",
      "binproviders": "env,puppeteer",
      "min_version": "149.0.0",
      "overrides": {
        "puppeteer": {
          "install_args": [
            "chromium"
          ]
        }
      }
    }
  ],
  "output_mimetypes": [
    "application/x-ndjson"
  ],
  "properties": {
    "CHROME_BINARY": {
      "type": "string",
      "default": "chromium",
      "description": "Path to a Chromium-family browser binary"
    },
    "CONSOLELOG_ENABLED": {
      "type": "boolean",
      "default": true,
      "x-aliases": [
        "SAVE_CONSOLELOG",
        "USE_CONSOLELOG"
      ],
      "description": "Enable console log capture"
    },
    "CONSOLELOG_TIMEOUT": {
      "type": "integer",
      "default": 30,
      "minimum": 5,
      "x-fallback": "TIMEOUT",
      "description": "Timeout for console log capture in seconds"
    }
  }
}
