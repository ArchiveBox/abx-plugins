{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "pip",
  "description": "Install Python-based binaries into a managed virtual environment.",
  "type": "object",
  "additionalProperties": false,
  "required_plugins": [],
  "required_binaries": [
    {
      "name": "python",
      "binproviders": "env",
      "min_version": null
    }
  ],
  "output_mimetypes": [],
  "properties": {
    "PIP_VENV_PYTHON": {
      "type": "string",
      "default": "",
      "description": "Preferred Python interpreter for creating the shared pip virtualenv"
    }
  }
}
