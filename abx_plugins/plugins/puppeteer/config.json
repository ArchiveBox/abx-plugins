{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Puppeteer",
  "description": "Install and manage Chrome for Testing through the Puppeteer toolchain.",
  "type": "object",
  "additionalProperties": false,
  "required_plugins": [],
  "required_binaries": [
    {
      "name": "puppeteer",
      "binproviders": "npm",
      "min_version": null,
      "overrides": {
        "npm": {
          "install_args": [
            "puppeteer",
            "@puppeteer/browsers",
            "abxbus@^2.5.4",
            "--min-release-age=0"
          ]
        }
      }
    }
  ],
  "output_mimetypes": [],
  "properties": {
    "CHROME_BINARY": {
      "type": "string",
      "default": "chromium",
      "x-aliases": [
        "CHROMIUM_BINARY"
      ],
      "description": "Path to Chromium, Chrome for Testing, or Chrome Canary binary"
    },
    "PUPPETEER_ENABLED": {
      "type": "boolean",
      "default": true,
      "description": "Enable Puppeteer dependency installation during crawl setup"
    },
    "PUPPETEER_TIMEOUT": {
      "type": "integer",
      "default": 900,
      "minimum": 60,
      "description": "Timeout in seconds for Puppeteer-managed browser dependency installation"
    }
  }
}
